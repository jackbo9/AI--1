# Phase 1 设计与联调待办

- [ ] 由品牌团队替换临时“飞书式”通用 SaaS 视觉：Logo、色板、字体授权、插画风格和海报模板。
- [ ] 确认飞书企业自建应用、OAuth 权限、机器人发消息权限与 HTTPS 测试域名。
- [ ] 在 `.env.local` 配置并验证 DeepSeek 的真实模型、额度和数据合规边界。
- [ ] 确认图片供应商的 OpenAI 兼容接口、模型、费用和内容保留政策；当前适配器期望 `/images/generations` 返回 `b64_json` 或 URL。
- [ ] 将本地 JSON 任务存储替换为 PostgreSQL，并让独立 Worker 进程承接任务。
- [ ] 增加图片模型成功路径、飞书通知失败路径与全流程 Playwright E2E 的真实凭据隔离测试。
- [ ] 评估两阶段主视觉管线：先用低成本图片模型生成草稿，再将选中的草稿作为参考图交给 GPT Image 2 精修；该能力必须是显式、可配置的二次操作，记录两个模型、提示词版本、输入资产与累计成本，不能成为当前默认生成路径。
