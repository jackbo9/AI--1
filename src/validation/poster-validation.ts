import type { EmployeeActivityInput, PosterDocument } from "@/contracts/poster";
export function validatePoster(input: EmployeeActivityInput, document: PosterDocument) { const messages: string[] = []; if (document.outputFormat !== input.outputFormat) messages.push("输出尺寸被意外改写"); if (JSON.stringify(document.sessions) !== JSON.stringify(input.sessions)) messages.push("场次、日期、时间或地点被意外改写"); if (document.audience !== input.audience) messages.push("参与对象被意外改写"); if (document.notice !== input.notice) messages.push("注意事项被意外改写"); if (document.includeQr !== input.includeQr || document.qrPayload !== input.qrPayload) messages.push("二维码配置被意外改写"); return { passed: messages.length === 0, messages }; }
