"use client";
import type { FormEvent } from "react";
import { t01PortraitSubtitleRecommendedCharacters } from "@/contracts/poster";
import { Field, SectionHead, TextField } from "../fields";
import type { FormState, QrMode, SessionState } from "../types";
import type { RenderTargetId } from "@/contracts/brand";

const targets: Array<[RenderTargetId, string, string]> = [["portrait_1080x1920", "竖版", "1080 × 1920"], ["landscape_1920x1080", "横版", "1920 × 1080"], ["banner_2227x950", "Banner", "2227 × 950"], ["longform_1080xAuto", "长图", "1080 × 自动"]];
type Props = { form: FormState; qrMode: QrMode; qrUploadPending: boolean; aiReady: boolean; aiCandidate?: { slogan: string; subtitle: string }; onToggleRenderTarget: (target: RenderTargetId) => void; onField: <K extends keyof FormState>(key: K, value: FormState[K]) => void; onQrUrl: (value: string) => void; onSession: (key: keyof SessionState, value: string) => void; onQrMode: (mode: QrMode) => void; onQrUpload: (file?: File) => void; onClearQrAsset: () => void; onAssist: () => void; onSubmit: (event: FormEvent) => void; pending: boolean; error?: string };

export function StepOne({ form, qrMode, qrUploadPending, aiReady, aiCandidate, onToggleRenderTarget, onField, onQrUrl, onSession, onQrMode, onQrUpload, onClearQrAsset, onAssist, onSubmit, pending, error }: Props) {
  return <form onSubmit={onSubmit} noValidate><section className="ead-section"><div className="ead-form-heading"><SectionHead index="01" title="填写海报信息" /><small><i>*</i>为必填项</small></div>
    <div className="ead-template-picker"><b>选择海报尺寸</b><small>可选择多种尺寸，右侧可切换查看。</small><div>{targets.map(([target, name, size]) => <button type="button" key={target} className={form.renderTargets.includes(target) ? "is-selected" : ""} onClick={() => onToggleRenderTarget(target)}><span className="ead-template-meta"><strong>{name}</strong><small>{size}</small></span><span className={`ead-format-icon is-${target.split("_")[0]}`} aria-hidden="true"><i /></span></button>)}</div></div>
    <div className="ead-grid"><Field className="ead-span-2" label="一级大标题" placeholder="例如：羽球挑战赛" required multiline value={form.activityName} onChange={(value) => onField("activityName", value)} /><Field className="ead-span-2" label="宣言标题" placeholder="例如：九号员工羽毛球赛 / BADMINTON" required value={form.slogan} maxLength={40} hint="格式：九号员工活动名称 / ENGLISH；可手填或由 AI 生成" onChange={(value) => onField("slogan", value)} /><Field className="ead-span-2" label="副标题" placeholder="例如：一起上场，热爱不设限" required value={form.subtitle} maxLength={40} hint={`建议 ${t01PortraitSubtitleRecommendedCharacters} 字以内；可手填或由 AI 生成`} onChange={(value) => onField("subtitle", value)} /></div>
    <div className="ead-copy-actions"><button type="button" className="ead-secondary ead-ai-action" onClick={onAssist} disabled={pending}>{pending ? "正在准备建议…" : "获取文案建议"}</button></div>
    {error && <p className="ead-error" role="alert">{error}</p>}
    {pending && <p role="status">正在处理文案，请稍候；建议生成后会显示在这里。</p>}
    {aiReady && aiCandidate && <div className="ead-ai-candidate"><b>文案建议</b><p><span>宣言标题</span>{aiCandidate.slogan}</p><p><span>副标题</span>{aiCandidate.subtitle}</p><small>点击“使用文案建议”即可应用。</small></div>}
    <div className="ead-session-block"><div className="ead-session-heading"><b>活动信息</b></div><div className="ead-grid"><Field label="01 比赛日期" required={!form.renderTargets.every((target) => target === "banner_2227x950")} type="date" value={form.session.date} onChange={(value) => onSession("date", value)} /><Field label="02 比赛地点" placeholder="例如：园区体育馆" required={!form.renderTargets.every((target) => target === "banner_2227x950")} value={form.session.location} onChange={(value) => onSession("location", value)} /><Field className="ead-span-2" label="03 参与对象" placeholder="例如：全体员工" required={!form.renderTargets.every((target) => target === "banner_2227x950")} value={form.audience} onChange={(value) => onField("audience", value)} /><TextField className="ead-span-2" label="04 赛事规则" placeholder="例如：小组循环赛，每场三局两胜" value={form.rules} onChange={(value) => onField("rules", value)} /></div></div>
    {form.renderTargets.includes("longform_1080xAuto") && <section className="ead-finalists"><div className="ead-session-heading"><div><b>决赛名单</b><small>组名可改为 2–4 字，每组最多 6 人；输入即时预览，未完整条目需补齐或移除。</small></div></div>{form.finalistGroups.map((group, groupIndex) => <div className="ead-finalist-group" key={groupIndex}><header><input aria-label={`第${groupIndex + 1}组名称`} value={group.label} maxLength={4} placeholder="2–4 字组名" onChange={(event) => onField("finalistGroups", form.finalistGroups.map((item, index) => index === groupIndex ? { ...item, label: event.target.value } : item))} /><small>{group.entrants.length} / 6 人</small></header><div className="ead-finalist-entries">{group.entrants.map((entrant, entrantIndex) => <div className="ead-finalist-entry" key={entrantIndex}><input aria-label={`${group.label}第${entrantIndex + 1}人姓名`} placeholder="姓名" value={entrant.name} onChange={(event) => onField("finalistGroups", form.finalistGroups.map((item, index) => index === groupIndex ? { ...item, entrants: item.entrants.map((person, personIndex) => personIndex === entrantIndex ? { ...person, name: event.target.value } : person) } : item))} /><input aria-label={`${group.label}第${entrantIndex + 1}人赛区`} placeholder="所属赛区" value={entrant.region} onChange={(event) => onField("finalistGroups", form.finalistGroups.map((item, index) => index === groupIndex ? { ...item, entrants: item.entrants.map((person, personIndex) => personIndex === entrantIndex ? { ...person, region: event.target.value } : person) } : item))} /><button type="button" aria-label={`移除${group.label}第${entrantIndex + 1}人`} onClick={() => onField("finalistGroups", form.finalistGroups.map((item, index) => index === groupIndex ? { ...item, entrants: item.entrants.length === 1 ? [{ name: "", region: "" }] : item.entrants.filter((_, personIndex) => personIndex !== entrantIndex) } : item))}>移除</button></div>)}</div><button type="button" className="ead-add-finalist" disabled={group.entrants.length >= 6} onClick={() => onField("finalistGroups", form.finalistGroups.map((item, index) => index === groupIndex ? { ...item, entrants: [...item.entrants, { name: "", region: "" }] } : item))}>＋ 添加名单</button></div>)}</section>}
    {form.renderTargets.some((target) => target === "portrait_1080x1920" || target === "landscape_1920x1080") && <div className="ead-qr-subsection"><div className="ead-qr-title"><b>二维码</b><small>选填；不添加也可以生成和下载海报</small></div><div className="ead-qr-options"><button type="button" className={qrMode === "none" ? "is-selected" : ""} onClick={() => onQrMode("none")}>○ 不添加</button><button type="button" className={qrMode === "add" ? "is-selected" : ""} onClick={() => onQrMode("add")}>＋ 添加</button></div>{qrMode === "add" && <div className="ead-qr-add">{!form.qrUrl.trim() && !form.qrAssetId && <p className="ead-field-hint">请填写链接或上传图片；不需要时可选择“不添加”。</p>}<div className="ead-qr-add-grid"><input type="url" placeholder="粘贴报名 URL（会生成二维码）" value={form.qrUrl} onChange={(event) => onQrUrl(event.target.value)} /><label className="ead-upload-label">{qrUploadPending ? "正在上传…" : "上传二维码图片"}<input type="file" accept="image/png,image/jpeg,image/webp" disabled={qrUploadPending} onChange={(event) => { onQrUpload(event.target.files?.[0]); event.currentTarget.value = ""; }} /></label></div><div className="ead-upload-status"><span>{qrUploadPending ? "正在安全校验并保存二维码图片…" : form.qrAssetId ? `已上传：${form.qrAssetName}（将进入最终海报）` : form.qrUrl ? "URL 将用于服务端生成二维码" : "支持 PNG / JPEG / WebP，最大 5MB，最小 96 × 96 像素"}</span>{form.qrAssetId && <button type="button" onClick={onClearQrAsset}>移除图片</button>}</div></div>}</div>}
  </section><button type="submit" className="ead-primary" disabled={pending || qrUploadPending}>{pending ? "正在处理…" : "确认文案，进入主视觉 →"}</button></form>;
}
