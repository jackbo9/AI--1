# AGENTS.md

本文件约束所有在本仓库中工作的编码代理和开发者。开始任何实现前，先阅读 `README.md`、`HANDOFF.md`、`docs/ARCHITECTURE.md` 和 `docs/CONTRACTS.md`。

## 项目目标

构建飞书企业内部行政海报生成工具。系统重点是高频物料的品牌一致性、信息准确性和低门槛使用，不是自由画布设计软件，也不是通用图片生成器。

## 已锁定架构

- 飞书企业自建应用，网页应用为主入口，机器人为通知和快捷入口。
- Next.js + TypeScript 构建 Web/API，Worker 异步执行生成任务。
- LLM 输出结构化 `PosterDocument`，必须经过 Zod/JSON Schema 校验。
- 图片模型只生成插画、背景和装饰素材。
- HTML/CSS + Playwright 负责确定性排版和 PNG/PDF 输出。
- PostgreSQL 是试点阶段的业务真源；多维表格仅为可选运营后台。
- 模型、飞书、存储和渲染必须通过 Provider/Adapter 隔离。

## 不可破坏的产品原则

1. 不允许图片模型生成 Logo、二维码、日期、地点或海报正文。
2. 不允许 LLM 返回任意 HTML/CSS 或绝对坐标控制模板。
3. 不可改写字段在 AI 调用前后必须逐字段校验。
4. 生成任务必须异步、幂等、可重试并保留不可变版本。
5. AI 插画失败时必须可退回默认品牌资产。
6. 重要文本不得通过裁切、隐藏或无限缩小静默解决溢出。
7. 每次结果必须能追踪模板、Prompt、模型和资产版本。
8. 飞书或多维表格通知失败不得破坏已完成的核心任务。

## 开发顺序

严格优先完成 `docs/IMPLEMENTATION_PLAN.md` 中的最小纵向闭环。除非用户改变范围：

1. 先做 Fixture → Schema → HTML 模板 → PNG。
2. 再做任务、版本和预览。
3. 再接 LLM。
4. 再接图片模型。
5. 最后接飞书身份、机器人和多维表格同步。

不要先生成大量 CRUD、后台页面、Provider 空文件或复杂基础设施。

## 建议目录职责

- `src/app`：页面和 HTTP 接口，不放模型编排细节。
- `src/contracts`：领域类型、Zod Schema、API 请求响应。
- `src/server`：鉴权、任务服务、数据库访问和权限判断。
- `src/worker`：异步步骤和状态机。
- `src/providers`：模型、存储、渲染供应商适配器。
- `src/integrations/feishu`：飞书 SDK、登录、机器人和卡片。
- `src/templates`：模板、模板元数据和 CSS；不得读取模型密钥。
- `src/validation`：内容与品牌规则。
- `tests/fixtures`：可复现输入，不能包含真实员工敏感数据。

## 编码约束

- TypeScript 开启严格模式，避免 `any`；外部响应先解析再进入领域层。
- 领域层不得依赖具体模型供应商 SDK。
- 服务端环境变量集中校验，禁止散落读取 `process.env`。
- App Secret、模型密钥、Webhook Secret 不得出现在客户端 bundle、日志、Fixture 或提交中。
- API 错误使用稳定错误码和用户可理解文案，不向客户端暴露堆栈。
- 数据库迁移必须可向前执行并说明回滚/兼容策略。
- 生成版本不可原地覆盖；修改会创建新版本。
- 外部调用必须设置超时、有限重试和可观测日志。
- 重试前确认操作幂等，避免重复收费和重复发消息。

## 海报模板约束

- 产品 UI 可以使用 Tailwind；最终海报模板使用受控 HTML/CSS。
- 模板必须声明尺寸、插槽、最大字数、溢出策略和支持语言。
- 字体必须等待加载完成后才截图。
- Logo 和二维码保持原比例，不允许 CSS 拉伸。
- AI 图片只能出现在模板声明的可变资产槽位。
- 每个模板至少包含正常、长文本和缺失字段测试。
- 有基线图时，修改模板必须运行视觉回归并人工查看差异。

## 飞书约束

- 使用当前官方 `@larksuiteoapi/node-sdk`，不要使用已废弃 SDK。
- 事件和卡片回调应快速确认，耗时工作进入 Worker。
- 验证飞书回调的签名、Token 和加密配置。
- 飞书用户使用稳定 ID 映射，不以姓名作为主键。
- 请求最小必要权限；新增权限前说明用途。
- 多维表格同步应是可关闭的 Adapter，不能成为核心任务硬依赖。

## 质量检查

每次提交前，按变更范围执行：

- TypeScript 类型检查。
- ESLint/格式检查。
- 相关单元测试。
- API 或 Worker 集成测试。
- 涉及模板时生成 PNG 并检查视觉回归。
- 涉及中文文本时检查缺字、禁则、换行和最小字号。
- 涉及飞书时验证失败、重复事件和权限不足路径。

不要声称“完成”而未说明实际执行的检查和未验证项。

## 范围与决策管理

- 当前 MVP 不做自由画布、视频、自动全员发布或模型训练。
- 不因编码方便改变业务场景、品牌规则和不可改写字段。
- 需要新增基础设施、模型供应商、飞书权限或外部 SaaS 时，先记录影响并请求用户确认。
- 架构决策发生变化时，同步更新 `HANDOFF.md`、`docs/ARCHITECTURE.md` 和相关契约。
- 遇到模糊需求时，优先用 Fixture 和验收条件把问题具体化。

## 仓库操作

- 使用 `rg`/`rg --files` 查找文本和文件。
- 使用 `apply_patch` 做手工文件修改。
- 保留用户已有更改，不使用破坏性 Git 命令。
- 不自动初始化 CodeGraph；如果项目尚未初始化且确实需要结构分析，先询问用户。

