# 九号行政智绘引擎｜方案 B Handoff

最后更新：2026-09-02  
状态：技术路径已锁定，业务场景与品牌规则待三方补齐

## 1. 项目目标

在飞书工作台中提供一款面向集团全球行政专员的“行政设计助手”。用户无需专业设计软件，通过少量结构化输入即可生成具备基本审美、品牌一致性和信息准确性的内部传播海报，并以 PNG/PDF 等形式使用。

项目解决的不是“无限创意设计”，而是高频、低到中等设计等级行政物料的标准化生产：

- 减少找模板、写文案和反复排版的时间。
- 锁定 Logo、字体、品牌色、信息层级和安全区。
- 允许在受控范围内生成与场景相关的插画和装饰元素。
- 支持多语言、多尺寸和多渠道输出。
- 沉淀业务场景、品牌资产、模板和生成规则。

## 2. 已锁定的产品与技术决策

### 2.1 飞书底座

采用企业自建应用，并在同一应用中逐步开启：

- 网页应用：工作台主入口、表单、预览、修改与导出。
- 机器人：快捷入口、任务状态、失败提醒和结果卡片。
- 开放接口：用户身份、消息、文件以及可选的多维表格读写。

多维表格的定位调整为：

- 模板、风格包和场景配置后台（可选）。
- 生成任务、审核结果和用户反馈运营后台（可选）。
- 早期 Demo 的临时输入入口。

多维表格不承担：核心任务状态真源、模型密钥管理、复杂重试、最终排版渲染。

### 2.2 生成方式

采用“AI 生成内容与视觉素材 + 代码确定性排版”，不采用一个不可控的“排版 AI”完成整张海报。

```text
用户输入
  -> 场景识别与字段校验
  -> LLM 输出结构化文案 JSON
  -> 模板和风格包选择
  -> 可选插画/背景生成
  -> HTML/CSS 确定性排版
  -> 品牌与内容规则校验
  -> 用户预览和局部修改
  -> PNG/PDF/多尺寸输出
```

### 2.3 视觉职责边界

- AI 可以生成：插画、背景、装饰图形、候选标题和本地化文案。
- AI 不可以生成：Logo、二维码、重要时间地点、海报正文文字图层。
- 代码锁定：Logo 安全区、字体、颜色、层级、字段位置、二维码和最低可读字号。
- Figma 用于模板设计和设计系统维护，不作为线上实时渲染器。

## 3. 合作分工

### 业务方

交付每个场景的内容协议：

- 场景定义、频率和优先级。
- 必填字段、选填字段和信息优先级。
- 不可改写字段与允许 AI 润色字段。
- 输出渠道、尺寸、语言和审核人。
- 真实历史案例、常见错误和成功标准。

### 品牌设计

交付可执行的视觉协议：

- 模板族、适用场景和组件规则。
- 品牌色、字体、Logo、安全区和禁用规则。
- 文字长度、换行、溢出和多语言处理原则。
- 固定资产、可变资产和插画风格包。
- 合格/不合格示例及人工验收标准。

品牌设计负责定义模型生成结果的质量目标，但模型供应商、接口和部署方式由品牌、产品与技术联合验证。

### UX/UI（本项目产品化负责人）

- 把业务字段转成低门槛输入流程。
- 建立“场景—模板—风格包—生成策略”的映射。
- 定义预览、局部修改、重新生成、导出和失败恢复。
- 将品牌规则转成模板约束、字段约束和校验规则。
- 定义内容、模板、生成任务和版本的数据契约。
- 组织可用性测试和质量指标评估。

### 技术与 AI 支持

- 飞书身份、权限、机器人及网页应用接入。
- 模型网关、异步任务、文件存储、日志和成本控制。
- HTML/SVG 渲染、品牌规则自动校验和多尺寸输出。
- 部署、监控、备份、安全与合规加固。

## 4. MVP 范围

### 必须包含

- 一个飞书工作台网页入口，测试阶段可先使用普通网页。
- 3 个代表性场景；最终由业务方确认。
- 每个场景至少 1 个可执行模板。
- LLM 输出结构化文案，而不是自由文本。
- 至少 1 个场景支持可选的 AI 插画。
- 预览、编辑关键文字、只换插画、重新生成和导出 PNG。
- Logo、品牌色、字段完整性和文字溢出基础校验。
- 任务状态、错误信息和生成历史。
- 模型与模板版本记录。

建议的三个代表性场景（未最终锁定）：

1. 员工活动：验证插画、标题感和活动信息卡片。
2. 员工通知：验证固定品牌骨架和信息准确性。
3. 员工福利：验证长文本、表格、二维码和模块化长图。

### 暂不包含

- 自由画布或接近 Figma/Canva 的编辑器。
- 视频、动态海报和整套社媒物料。
- 模型微调或自训练。
- 自动发布到全部群聊或全员渠道。
- 无人工确认的直接发布。
- 生产级高并发、多区域容灾和完整商业化后台。

## 5. 核心用户闭环

1. 用户从飞书工作台或机器人进入。
2. 选择海报场景，而不是从空 Prompt 开始。
3. 填写必要信息并选择语言、尺寸和视觉风格。
4. 系统校验不可缺失或不可改写字段。
5. 系统生成文案、插画和模板预览。
6. 用户可编辑文字、换模板或只重新生成插画。
7. 系统运行品牌与信息校验，显示可理解的修复建议。
8. 用户确认后导出 PNG/PDF，并收到飞书结果卡片。
9. 系统记录版本、耗时、模型、模板及用户反馈。

## 6. MVP 验收指标

### 技术

- 同一结构化输入在同一模板上可以稳定复现排版。
- 重要字段不由图片模型生成，且不会被 LLM 擅自修改。
- 生成失败不会产生半完成记录；任务可安全重试。
- 模型超时、插画失败时可以降级到默认品牌资产。
- 输出图片无缺字、乱码、Logo 变形和二维码失真。
- 每个结果可追踪输入、模板版本、Prompt 版本和模型版本。

### 产品与业务

- 用户无需理解 Prompt 工程即可提交任务。
- 首次生成到可用结果的中位时间可被记录。
- 一次通过率、平均修改次数和用户满意度可被统计。
- 设计师能判断不合规来自模板、资产、模型还是内容输入。

## 7. 关键风险

- 公司是否允许创建和发布飞书企业自建应用。
- 飞书工作流、机器人、网页应用及开放接口的企业权限。
- 公司模型、外部模型和生成内容的数据边界。
- 品牌字体服务端渲染与全球区域使用许可。
- 插画模型的风格稳定性、版权和成本。
- 长文本和多语言导致模板溢出。
- Chromium 渲染的 CPU/内存消耗。
- 无技术支持时，Demo 容易被误认为生产系统。

## 8. 启动前待确认事项

### 飞书与 IT

- [ ] 可创建企业自建应用并发布给测试用户。
- [ ] 可开启网页应用和机器人能力。
- [ ] 确认工作台页面域名、HTTPS 和回调地址要求。
- [ ] 确认所需通讯录、消息、文件和多维表格权限审批人。
- [ ] 确认测试环境、公司对象存储和数据库资源。

### AI 与安全

- [ ] 可用的 LLM 与图片生成模型清单。
- [ ] 模型网关地址、额度、超时和内容限制。
- [ ] 是否禁止员工信息或内部活动数据进入外部模型。
- [ ] 日志中哪些输入必须脱敏或禁止记录。

### 业务与品牌

- [ ] 首期三个场景及真实测试案例。
- [ ] Logo、品牌色、字体和许可说明。
- [ ] 每个场景的字段协议和信息优先级。
- [ ] 模板、风格包和默认降级资产。
- [ ] 自动校验和人工验收规则。

## 9. 官方参考

- 飞书企业自建应用开发流程：<https://open.feishu.cn/document/home/introduction-to-custom-app-development/self-built-application-development-process>
- 飞书网页应用接入：<https://open.feishu.cn/document/home/integrating-web-apps-in-5-minutes/create-app-and-configuration>
- 飞书机器人开发：<https://open.feishu.cn/document/home/develop-a-bot-in-5-minutes/create-an-app>
- 飞书官方 Node SDK：<https://github.com/larksuite/node-sdk>
- 多维表格开放接口概览：<https://open.feishu.cn/document/server-docs/docs/bitable-v1/bitable-overview>

