# 体育赛事 Demo｜今日轻量修改指南

日期：2026-09-04。本文为实施指南，任务尚未执行；不代表新增功能已经上线。

依据：`uploads/转写_行政智慧引擎讨论的1个录制文件.zip` 内 `20260904165909-转写_行政智慧引擎讨论-转写原文版-1.txt`，以及本次对话中设计同事提供的体育主视觉 prompt。会议转写按语境理解“长途”为长图，不采纳其中误转写的尺寸。模板尺寸以当前 Figma 与代码为准。

## 今天只交付什么

让同事走通：体育赛事报名信息 → 确认文案 → 输入并确认体育画面方案 → 一张主视觉 → 四种规格预览和下载。

复用已有四步流程、表单、T01 模板、上传二维码及独立输出。今天不增加新的页面、复杂配置平台、模型供应商或自动扩图。

## 会议结论与范围

- 03:18–05:38、11:11–11:51：报名阶段的信息基本足够，重点为主题、说明、比赛时间地点、参与对象/报名要求、规则与报名方式。保留现有必填/选填划分，不再增加任意自定义模块。
- 05:38–11:43：开赛通知、决赛名单、各赛区精彩回顾和赛果公示分阶段发生。不能因为将来要做名单长图，现在就要求报名者填写名单、成绩和照片。倒计时没有被确认为需求。
- 15:44–18:29：长期倾向复用一套模板，但其他活动能否共用尚待验证；今天只开放体育赛事，不解锁下午茶、通知、问卷。
- 18:52–19:27：希望保持同一主视觉，通过裁切/延展适配。当前代码只有裁切，没有扩图；不能把 prompt 中的“可延展”宣传成已实现扩图。
- 19:35–20:39：赛事、颜色和表现方式用自由文本表达并给示例，避免固定赛事选项限制新增项目；尺寸在排版导出阶段选择。

## 按顺序执行的五项任务

- [ ] **1. 明确试用范围和视觉输入。** 修改 `src/components/activity-studio.tsx`，复用现有 Step 3 文本框和按钮。入口标明“体育赛事报名 · 试用”；视觉输入提示改为“例如：羽毛球，蓝色，拍面击球瞬间；颜色和表现方式可不填”。不增加三个必填字段，不让用户粘贴整份系统 prompt。允许“篮球”等简短有效赛事输入；同步检查 `refineVisual` 的前端限制和 `refineVisualSchema` 的接口限制，不能只放开按钮。空输入或过长时显示原因/字数；已有已确认的画面描述仍按完整描述规则校验。验收：新赛事可输入，简短赛事能优化，空输入不会无解释置灰。

- [ ] **2. 接入体育视觉规则，移除旧默认值冲突。** 修改 `src/providers/prompt-compiler.ts`、`src/providers/illustration-provider.ts`，复用当前 Compiler 和图片 Provider。提炼设计 prompt 为一份版本化的体育规则：真实运动摄影、1–3 个核心符号、单主色、器材/运动瞬间/局部发力、避免面部主导和团建摆拍。LLM 只根据本次赛事选择相关元素；颜色/表现未填时自动给建议并展示给用户，不发送整套网球/篮球/羽毛球/拔河例文。体育路径移除“企业同事必出镜”“固定黑白灰＋行政黄”“允许卡通”等冲突默认，品牌 Logo 的正式资产不变。验收：羽毛球器材特写不会被默认改成人物合照，指定蓝色不会被行政黄覆盖。

- [ ] **3. 确认内容真正决定最终生图。** 同上两文件，并核对 `src/worker/run-job.ts`。当前 `briefFromConfirmedDescription` 会重置 subject/palette，并把描述前 80 字重复作为 action/setting，这是今日必须改的关键点。最轻方案是将用户确认的画面描述作为最终 prompt 的创意正文，单独拼接体育风格与构图规则；不要再拆成几段截断文字或重新加入人物/颜色默认值。需要保留结构化 brief 时，仅用于记录并明确与确认文本的关系，不再用旧默认值覆盖正文。用户修改后不再隐式调用第二次 LLM。检查最终 prompt 总长度（现为 2200）并在调用前给出可理解错误；不要靠静默截断解决。更新并记录 prompt 版本。验收：确认文字中的器材、颜色、摄影语言能完整进入最终请求；二维码开关、链接和上传资产不改变生图正文。

- [ ] **4. 四尺寸先维持一图裁切。** 复用 `t01-extra-renderer.ts`、`t01-wide.ts`、`t01-longform.ts`、`t01-output-gallery.tsx`；本项主要是核对样张和界面说明，不要求重写渲染器。生成一张原始素材，其余规格复用它；保留“不再次生图、横向裁切可能改变主体呈现”的说明。生成约束描述主体居中偏右、自然低纹理外围；不要加入“这里画/留二维码、Logo、标题”等正向排版用途。现有竖版底部信息区仍需留白，不能只照搬左上留白。936×780 是长图内图片槽，长图输出仍是 1080×自动高度。验收：同一任务四图主视觉一致，未新增三次图片请求；裁切明显破坏主体的样例记录问题，不冒充适配通过。

- [ ] **5. 验收后保存并发出试用入口。** 先跑类型检查、Lint、相关单测；增加“颜色/主体不被覆盖、二维码不进入生图、简短赛事输入”回归。人工先完整跑通一条羽毛球任务：确认文案 → 视觉确认 → 四规格下载 → 刷新恢复 → 竖版/长图扫码。再用篮球或拔河抽查一条新视觉，不重复跑无关大测试。检查空选填项、一次双场次/长规则的本地渲染。构建与开发服务隔离，避免同时写 `.next`。当前二维码上传、确认文案和 prompt 修复还有未提交内容，收口时审查后统一保存回退提交，排除密钥、会议原文、上传文件及生成图片。第二位同事实际登录并验证任务/图片/二维码资源隔离后，再扩大发送范围。

## 今天已知限制，直接说清楚

1. **报名长图不是名单/赛果长图。** 当前长图只展示同一次报名的详细信息；决赛名单、赛区照片、结果公示待设计同事整理过往案例后另做字段和模板对照。
2. **横版二维码存在需求缺口。** 会议 05:20 提到横版报名需要二维码，但现有横版 Figma 实现没有该槽位。今天建议将横版/Banner 标为宣传物料，报名入口用竖版/长图承载；这是临时范围建议，不是会议已同意取消横版二维码。若本次试用必须单发横版完成报名，则须先补设计确认的二维码槽，不能直接叠在任意位置。
3. **颜色与图像风格可优化，弱 fallback 决策不变。** 不恢复整张换背景或重遮罩；真实生图失败仍可使用明确标注的默认资产。背景二维码自动检测、上传二维码自动解码暂不加，今日实际扫码验收。
4. **发链接不等于已经上云。** 今日最快可在当前办公局域网邀请少量同事，电脑保持开机、服务在线，先确认对方能访问。跨网络持续试用需要公司服务器或另行确认云资源、HTTPS 与飞书回调；不要把购买/迁移阿里云并入今日 prompt 小改。

## 建议试用反馈只收四项

赛事与画面想法；卡在哪一步/报错截图；哪个尺寸出现裁切或文字问题；下载图是否可扫码。使用虚构报名信息即可。分享文案可写：“体育赛事报名海报 Demo，支持四规格；其他活动、名单/赛果和自动扩图暂未开放。”

## 交付判断

任务 1–3 完成、任务 4 无明显主体裁坏、任务 5 完成一条真实链路和另一账号检查后，可发小范围试用。不要为了今天上线同时增加自由编辑、多场景、多母版、自动扩图或新的后台。
