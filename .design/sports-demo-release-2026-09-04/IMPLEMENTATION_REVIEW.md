# 本轮执行与样张记录

2026-09-04。用户补充决定：前端不标记体育赛事，不增加专用入口；设计构图不叠加旧版上部/底部整片禁区。

已实现：确认文本独立保留并直接进入图片请求；去掉确认阶段固定人物、行政黄及重复截断字段；精简视觉指导；优化草稿显式呈现颜色；长度超限明确报错；optional 字段兼容旧任务；VISUAL_STYLE_MODE 可回退附加指导。前端未修改，短输入门槛/字数提示没有纳入本次代码修改。

验证：类型检查、ESLint、72 项单测通过。`scripts/compare-visual-direction.ts` 同一蓝色羽毛球器材需求，新旧规则各调用图片模型一次；后续通过 `--render-existing` 仅重排现有素材。首轮测试标题超出竖版单行容量被正确拦截，改用“羽球挑战”后四规格完成渲染（1080×1920、1920×1080、2227×950、1080×3024）。测试复用了通用 fixture 的部分文字，不作为正式报名文案。

原图、实际 prompt 与 source 记录见 `tmp/visual-direction-review/`；原始生成资产在 `data/generated/visual-direction-*-illustration.jpg`。竖版成品：`data/generated/direction-portrait-1788516674898.png`。其余三规格在该 tmp 目录。未调用第二次文案模型重写确认内容。

人工查看结论：两张原图均保持器材而非人物主体；新图蓝色背景更明确。只有一对随机样本，不足以证明稳定提升，也未充分实现运动瞬间感。新图四规格保持同一视觉；横版/Banner 有羽毛裁切，竖版拍柄进入底部时间标题区域，横版详细文字与球拍相交。没有重新添加遮罩、整张换底或整片底部禁区。

限制：本轮未执行完整飞书浏览器链路、手机扫码、第二账号验收或生产构建；未部署到云端、未对外发送。四规格“可生成”不能等同“四规格视觉均通过”。设计指导已接入，试用前需接受/处理上述图文冲突。实际框架目前仍为裁切，未实现扩图。

后续建议：先让设计同事评审这组真实样张，再单独决定裁切或模板信息区的调整；不继续靠 prompt 偷加禁区。切换 legacy 时需要重启服务，仅影响后续任务；已有结果保持不变。
