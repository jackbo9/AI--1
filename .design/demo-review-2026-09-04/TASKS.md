# 下一轮修改任务：四步 Demo 流程稳定与试用闭环

日期：2026-09-04。状态：已执行；二维码图片上传按用户明确要求保留为前端待办入口。

依据：用户已确认的 `docs/DEMO_UI_VISUAL_HANDOFF_2026-09-04.md`、`docs/DEMO_REVIEW_2026-09-04.md`，以及新增 `brief.composition` 超过 420 字的失败反馈。执行本清单，无需重新讨论已经确定的产品范围。

## 目标与边界

让同事能够稳定完成“填写事实 → 确认文案 → 优化并确认视觉 → 预览下载”，遇到失败时知道原因、保留输入并能继续。

继续只做 T01 中文单竖版 1080×1920；事实与创意分开；二维码仅链接；其他尺寸预留并注明后续开放；保留弱 fallback 试用方向。不增加四尺寸生成、KV 上传、局部修图、自由画布、新模型供应商或消融重构。

执行前读取仓库 AGENTS 与现有交接、架构、契约，重新检查 HEAD 和工作区，保护其他 agent 的更改。审查最后核对基准为 `1d4fe9a`，该提交已修复视觉优化 Worker 不接受 `REFINING_VISUAL` 的问题，勿重复修复旧根因。

## 第一批：先解除流程阻断

- [x] **R1 · 修复 composition 超长与错误恢复。** 修改现有 Prompt Compiler、brief/确认契约、Worker、Step 3 错误展示，复用图片 Provider；不改页面设计。对应审查第 11 项。
  - 用户确认描述最大 420 字，固定构图约束当前 236 字，拼接后最多 657 字；本地任务已确认相同错误。该问题按当前发布阻断项处理。
  - 分开“用户创意描述”和“系统版式约束”，固定约束只在最终 prompt 组装时注入一次。优化草稿仅展示可理解、可编辑的创意，不回填坐标、安全区等系统段落。
  - 建立明确的字段预算和 Schema；最终 brief/prompt 校验前置到图片调用前。不得靠提高单个上限或盲目截断蒙混通过，也不增加一次隐藏 LLM 调用。
  - 兼容已有含系统约束的草稿；新增编译版本标识，不原地覆盖历史结果。旧任务转换必须确定性处理，不能删除用户合法创意。
  - 将 Zod 错误映射成稳定错误码和中文提示；保留视觉输入并允许修正/重试。若历史失败任务已有可用图片，应先检查复用条件，避免恢复元数据时无必要地重复付费生图；新创意不能误用旧图片。
  - **验收：** 183/184/419/420 字描述均覆盖；421 字友好拦截；AI 优化生成的草稿满足确认容量；旧草稿不重复注入约束；无效载荷图片调用为 0；正常链路不会生成完图片后才因 brief 长度失败。

- [x] **R2 · 修复页面跳转、返回与刷新恢复。** 修改 ActivityStudio、Step 1–4、任务查询/错误处理与登录返回；新增一个轻量工作流 hook/reducer，复用现有布局和控件。对应审查第 1、2 项。
  - 分开 `viewStep`、后台状态和可访问步骤；有 job URL 时先展示恢复态，避免先出现无关默认表单。
  - 覆盖文案排队/生成、视觉优化、视觉确认、生图、渲染、完成和失败状态；同状态轮询不抢回用户视图，编辑文字不触发跳转。
  - 一次恢复原始事实、已确认文案、视觉输入及草稿；Step 3 返回 Step 2 可查看并继续，不能永久 loading 或调用错误状态的确认接口。
  - 新建任务原子清理旧编辑态；旧请求结果不能污染新任务。增加动作 pending、错误提示、防重叠轮询和重试。
  - OAuth 回调恢复合法站内任务地址；不扩大为任意 URL 跳转。
  - **验收：** 每个后台状态刷新；3→2→3；A→B 两任务隔离；断网、401、403、404、失败任务恢复；不触发额外模型调用。

## 第二批：确认什么，就生成什么

- [x] **R3 · 统一事实、可编辑文案和最终输出。** 修改 Copy Provider、confirm-copy、内容校验、Step 2、Preview 和模板字段投影；复用现有事实表单与 T01 槽位。依赖 R2 的恢复边界。对应审查第 3 项及第 10 项空内容行为。
  - 原文读取 `job.input`，确认后内容单独保存；补充说明投影到实际输出的顶部说明，避免 summary 更新却继续显示旧 subtitle。
  - 保存规则/奖品编辑值，不再用原始输入覆盖；规则与参与方式统一一次投影。截止时间、联系人、奖品本期仍不增加海报槽位。
  - 服务端验证主题、日期、时间、地点、参与对象及其他锁定事实，不能只依赖只读控件。
  - 全部可选叙述为空时不调用文案模型；移除旧 prompt 的编造默认项要求和固定兜底副标题，空值贯穿确认、预览和输出。
  - **验收：** 修改说明/规则/奖品后确认、刷新、生成值一致；篡改事实被拒绝；全空可直接确认且无编造正文。

- [x] **R4 · 在模型调用前拦截模板容量问题。** 修改 T01 容量预检、创建任务/确认入口和 Step 1 提示，调整 Preview 使其与实际模板规则一致；保留模板字号、单行标题和槽位。对应审查第 4 项。
  - 以真实字体、字重和宽度建立可复用检查，替换前端不准确的“24 字”判断；服务器同样执行。默认示例必须通过同一检查。
  - 优化后的可选文本也要在付费生图前进行适用的容量检查；超限明确返回可编辑步骤，不截断重要文本。
  - **验收：** 默认标题示例、长中文、中英混排、空内容、双场次；超限时图片调用为 0；合法预览和 PNG 不出现行数规则分歧。

- [x] **R5 · 绑定视觉草稿版本，并保证确认动作只执行一次。** 修改 Step 3、视觉优化/确认/只换主视觉接口、统一任务存储更新边界；复用现有 Worker 与 Provider。依赖 R1、R2；R3 产生新文案版本时接入同一失效机制。对应审查第 6、8 项。
  - 绑定 job、来源内容版本、视觉输入与草稿版本；改想法使旧稿失效，再优化安装新草稿，清空输入不自动填回。
  - 只换主视觉带入上次最终描述；按钮可用性与服务端状态一致，最终生成创建新版本并保留旧结果。
  - 同一动作重试复用幂等键，客户端立即锁定；服务端状态检查、幂等校验、认领执行须原子化。
  - 当前单进程 JSON 存储统一串行化读改写与原子替换，避免不同任务互相覆盖；不以局部按钮禁用替代服务端保证，不在本轮扩建分布式队列。
  - **验收：** 修改后不能确认旧稿；重复/并发确认使用 mock Provider 且图片只调用一次；不同任务并发更新不丢记录；只换主视觉无 409 死路。

- [x] **R6 · 完成视觉脱敏与风格一致性。** 修改编译输入投影、确认后过滤和本地规则兜底，复用现有品牌与安全区约束。依赖 R1，沿用 R3 的已确认内容。对应审查第 7 项。
  - 优化使用已确认内容的脱敏语义；图片调用前再次确定性过滤，不直接拼入原始日期、精确地点、联系信息、二维码链接或海报正文。
  - 用户明确风格在 AI 优化、本地兜底和最终编译中一致；处理“不要卡通”等否定表达，避免摄影常量覆盖插画意图。
  - **验收：** 虚构敏感字段不出现在最终 prompt；摄影/插画/否定卡通/本地兜底均覆盖；固定约束只注入一次；最终确认不再隐式调用 LLM 改写。

## 第三批：让试用结果可见、可下载、可反馈

- [x] **R7 · 贯通 trial 下载和真实质量提示。** 修改图片访问接口、输出资格映射、Step 4 与下载控件，复用现有弱遮罩选择及所有者校验。可独立修复；真实全链路验收依赖第一、二批。对应审查第 5、9 项。
  - 文件接口按具体版本 `exportAllowed ?? passed` 判断，不依赖当前全局 trial 配置追溯放开旧失败结果；禁止状态真正禁用下载控件。
  - 质量面板读取结构化真实检查，不搜索 messages 中的“Logo”判断失败、不固定显示尺寸通过；未知项隐藏或明确未检查。
  - 核实运行服务采用 `READABILITY_MODE=trial`：成功图片保留，遮罩 0/0.12，只有对比度警告时可下载；事实、字体、Logo、溢出等硬错误仍阻断。图片服务真正失败仍用默认品牌资产。
  - **验收：** trial 对比度警告成品可预览和下载；strict/硬失败被阻止；旧版本保持资格；他人访问被拒绝；正常 Logo 不误报。

- [x] **R8 · 清理原型残留和表单误导。** 修改 Step 1、标题区、尺寸入口、Field/TextField；复用当前 UI 设计系统，不重新设计布局。对应审查第 10 项其余内容。二维码图片上传按用户明确要求保留，标记为后端接入待办。
  - 删除二维码图片上传，仅保留 HTTP(S) URL；“不添加”清理 payload 和预览状态，空白 URL 按未填处理。
  - 删除“草稿自动保存”“两张母图、四种尺寸联动”等未实现承诺；预留入口禁用并注明后续开放。
  - 在填写处说明截止时间、联系人、奖品当前竖版不展示；补齐 label/id、键盘焦点和错误关联。
  - **验收：** 有/无二维码、添加后取消、刷新恢复一致；未实现能力无可用假入口；桌面与手机宽度可操作，字段有可靠可访问名称。

## 发布前复核

- [x] **R9 · 验收并记录可退回版本。** 复用现有测试/fixture/渲染脚本，补充有实际风险的流程与接口测试；更新 HANDOFF、相关契约与本清单状态。依赖 R1–R8。
  - 类型检查、ESLint、相关单元和 mock 集成测试、生产构建；正常/长文本/缺字段/双场/二维码以及 trial 可读性 PNG 人工查看。
  - 跑通一条真实飞书任务；验证刷新、文案确认、视觉确认、结果预览与下载。尚未实测的项目明确列出，不能以“单测全绿”代替。
  - 核对局域网 3000 端口入口与登录回调，按原授权建立修复提交和试用回退节点；保留原改版前标签，排除密钥、本地任务数据及生成产物，不强推。
  - **交付记录：** 提交 SHA、运行地址、回退方式、逐项验收结果、仍有的限制。一次交付一个可验证修复块，避免大改后无法判断哪处解决了问题。

## 当前复用关系与检查基线

直接复用：Next.js/TypeScript 工程、浅色工作台设计系统、品牌资产、T01 几何、图片 Provider、飞书身份和所有者校验。需要修改的现有组件：ActivityStudio、StepOne/Two/Three/Four、Preview、Field/TextField、LoadingCard。建议新增仅为工作流 hook/reducer、统一编译/校验边界和针对性测试，不另起一套页面。

本轮记录：`npm run typecheck`、`npm run lint`、`npm test`（45/45）、`npm run build`、`npm run render:t01-readability-review`（4/4）通过；3000 端口入口返回站内 Feishu 登录跳转，未使用未授权接口。未实测项目：真实 Feishu 登录后完整生图下载、第二位用户越权访问、真实图片模型付费调用、移动端真机浏览器；这些不以单测通过替代。回退点为 `1d4fe9a` 前的 `checkpoint-2026-09-04-pre-ui-visual-split` 标签，当前稳定修复提交另见 HANDOFF。
