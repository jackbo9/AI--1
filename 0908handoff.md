# 2026-09-08 T01 竖版流程 Handoff

## 今日完成

### 1. 合并文案填写与确认

- 将原 01、02 合并为同一页，填写时同步显示 T01 竖版预览。
- 字段按模板顺序收敛为：
  - 一级大标题：必填，保持用户原文，AI 不得改写。
  - 宣言标题、副标题：选填。
  - 比赛日期、比赛地点、参与对象、赛事规则：必填。
  - 二维码：选填，支持链接或上传图片。
- 新任务不再收集具体时间、第二场、补充说明、截止时间、联系人和奖品。
- AI 只辅助生成宣言标题和副标题；再次生成时先展示候选，不静默覆盖用户修改。
- 不使用 AI 也可以直接确认文案。
- 表单内容可在刷新后恢复；旧任务的历史字段仍可兼容读取。
- 保留 T01 标题宽度 952px、高度自适应、标题与副标题间距 22px 等正式模板规则。

### 2. 增加非空基础视觉描述

- 确认文案后直接生成可编辑的基础视觉描述，不再展示空白输入框。
- 描述结合大标题、赛事类型、活动信息和 T01 构图要求，包含主体、风格、色彩及标题安全区。
- 支持用户直接编辑或点击“AI 优化”。
- 描述与来源文案版本绑定并持久化；文案变化后旧描述不再保持已确认状态。
- “确认描述”和“确认主视觉”已拆分为两个明确动作。
- 图片模型仍只生成无文字主视觉，不生成标题、Logo、二维码和活动事实。

### 3. 更新体育赛事主视觉标准

- 默认描述和最终图片 Prompt 统一采用品牌设计方确认的体育 Campaign 标准。
- 自动识别羽毛球、网球、篮球、足球、排球、乒乓球和拔河等赛事。
- 默认使用真实体育摄影、运动器材、运动轨迹和场地结构，不使用 AI 概念图、低质 3D 或普通团建照片。
- 默认禁止人物、人体、手脚、面部、合影、文字、数字、Logo、二维码和水印。
- 固定构图逻辑为：
  - 左上：标题安全区。
  - 中右：主视觉核心，约 X 68%–78%、Y 48%–58%。
  - 外围：可裁切、延展背景。
- 顶部流程统一为 1「填写与确认文案」/ 2「生成主视觉」/ 3「查看与下载」。

### 4. 支持多个主视觉方案

- 每次生成新增一个不可变方案，不覆盖此前成功方案。
- 每个方案保存图片、生成描述、来源文案、Prompt、Provider 和模型信息。
- 支持沿用同一描述再次生成，也支持修改描述后生成新方案。
- 支持方案切换、选中状态和刷新恢复。
- 生成完成后停留在视觉阶段；只有点击“确认主视觉”才进入正式排版。
- 失败不会删除已有方案；幂等控制避免同一次操作重复调用图片模型。

### 5. 增加轻量完整海报预览

- 在步骤 02 点击任一主视觉方案后，右侧立即显示完整 T01 竖版海报。
- 方案选择采用前端乐观更新，切换时不再额外等待一次任务刷新。
- 预览直接使用受保护图片 URL 和 React/CSS 合成，不再传输包含字体、图片的超大 HTML。
- 切换方案时不启动 Chromium，不再长期停留在“正在同步模板…”。
- 浏览器 Canvas 对实际 `cover` 裁切后的标题安全区进行小尺寸像素采样，自动选择：
  - 黑色或白色标题。
  - 黑色或白色宣言标题。
  - 黑色或白色分隔线。
  - 正常或反白公司 Logo。
- 链接二维码在浏览器本地生成，上传二维码继续读取私有资源。
- 该颜色判断只用于快速预览；确认后仍由正式模板执行完整排版和可读性检查。

### 6. 完成最终页一屏预览

- 确认主视觉并完成正式排版后，默认在可用屏幕高度内完整展示 1080×1920 海报。
- 海报不依靠裁切、隐藏或无限缩小解决内容问题。
- 品牌检查、开始新海报、只换主视觉和下载操作保持可见。
- 提供“适合屏幕”和“放大查看”：
  - 适合屏幕：完整显示海报。
  - 放大查看：只在预览容器内滚动检查细节。
- 页面预览缩放不改变下载文件，下载仍为正式 1080×1920 PNG。
- 最终页面和下载使用同一个正式输出文件，不在浏览器重新生成海报。
- 已实测 1366×768 和 1440×900 桌面窗口，完整海报和下载操作均在一屏内可见。

### 7. 修复与简化

- 删除视觉阶段依赖的大体积模板预览路径，避免单次返回约数十 MB 的字体、图片和 HTML。
- 移除最终页对旧多尺寸输出接口的额外读取，解决“服务返回了无法解析的响应”。
- 保留正式 Playwright 渲染、1080×1920 输出和服务端品牌校验。
- 本轮未修改其他模板、员工福利模板和长图专属字段。

## 今日主要提交

| Commit | 内容 |
| --- | --- |
| `a543d04` | 合并 T01 文案填写与确认 |
| `959550a` | 约束主标题及可选标题契约 |
| `ad7dde0` | 保护不可改写事实字段 |
| `42ccc40` | 对齐填写预览和文案字段 |
| `22d3e05` | 稳定 T01 文案预览 |
| `1e69a56` | 提供基础视觉描述 |
| `54b4c9e` | 对齐体育赛事视觉 Prompt |
| `da1117c` | 保留并选择多个主视觉方案 |
| `1288d8f` | 回退响应较慢的重型预览实现 |
| `6d03063` | 增加轻量海报预览和黑白自动适配 |
| `422aabc` | 最终竖版一屏展示及局部放大 |

当前分支：`codex/update-image-generation-logic`  
当前远端：`origin/codex/update-image-generation-logic`  
当前功能提交：`422aabc`

## 已执行检查

- TypeScript 类型检查通过。
- ESLint 通过。
- 生产构建通过。
- 多方案 Worker、选择接口、竖版预览等相关单元测试通过。
- 4 项 Chromium T01 排版测试通过，包括长标题、空可选标题、信息区和二维码安全列。
- Fixture 浏览器实测通过：
  - 1366×768：整张竖版及下载操作在一屏内。
  - 1440×900：整张竖版及下载操作在一屏内。
  - 放大模式只滚动预览区域。

## 待办项目

### P0：真实业务验收

- 使用真实飞书账号和真实图片模型完整走一遍：
  - 手动填写并确认文案。
  - AI 辅助生成宣言标题和副标题。
  - 修改并确认基础视觉描述。
  - 连续生成至少两个主视觉方案。
  - 选择第一个方案并确认最终输出确实使用第一个方案。
  - 下载 PNG 并与页面逐项核对。
- 覆盖浅色、深色和高纹理主视觉，确认轻量黑白判断符合设计预期。
- 验证链接二维码和上传二维码在正式成品中的显示与可扫描性。
- 验证刷新、返回步骤和重新进入任务后的文案、描述、方案及选择恢复。

### P0：测试基线清理

- `npm test` 当前仍有一项既有契约用例需要统一：
  - `tests/unit/poster.test.ts` 期望“缺少地点”被拒绝，但当前 Schema 行为与测试预期不一致。
- 调整 Vitest 范围，避免把 `tests/e2e/*.spec.ts` 当作 Vitest 测试收集；Playwright E2E 应独立运行。
- 在 CI 中分别固定运行单元测试、Chromium 排版测试和 Playwright E2E。

### P1：预览与正式颜色一致性

- 当前步骤 02 的黑白判断是浏览器端快速近似，正式输出仍使用完整服务端检查。
- 需要用更多真实图片确认两者在复杂光影、局部高亮和高纹理背景下的一致性。
- 如差异明显，可将轻量像素算法抽成前后端共享规则，但不要恢复点击方案时启动 Chromium 的重型路径。

### P1：最终页补充验收

- 补测更小桌面窗口、浏览器缩放 80%/125% 和常见笔记本分辨率。
- 补测超长规则、三至五行标题、空宣言、空副标题以及二维码组合。
- 补充最终页“适合屏幕／放大查看”的自动化浏览器回归测试。

### 暂不纳入

- 其他员工活动模板刷新。
- 多模板选择和批量生成。
- 员工福利模板。
- 长图专属字段重构。
- 自由画布或任意版式编辑。

